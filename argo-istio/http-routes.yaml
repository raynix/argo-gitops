apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
    - name: kubernetes-gateway
  hostnames:
    - argo.awes.one
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
