apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wordpress-sophie
  namespace: istio-system
spec:
  secretName: wordpress-sophie-cert
  duration: 2160h0m0s # 90d
  renewBefore: 360h0m0s # 15d
  subject:
    organizations:
      - raynix.info
  privateKey:
    algorithm: RSA
    size: 2048
  usages:
    - server auth
    - client auth
  dnsNames:
    - sophie.raynix.info
  issuerRef:
    name: letsencrypt-issuer
    kind: ClusterIssuer
